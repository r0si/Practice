#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e6+10;
int a[maxn],n;
int main(int argc, char const* argv[])
{
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }

  return 0;
}
